{
  "name": "@codex-hud/buddy-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --persist-to=.wrangler/state --port 8787",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply codex_hud_buddy --local",
    "migrate:remote": "wrangler d1 migrations apply codex_hud_buddy --remote",
    "playwright:test": "playwright test -c playwright.config.ts"
  },
  "dependencies": {
    "@codex-hud/config": "0.1.0",
    "@codex-hud/buddy": "0.1.0",
    "@codex-hud/core": "0.1.0",
    "@codex-hud/db": "0.1.0",
    "@codex-hud/telegram": "0.1.0",
    "hono": "4.8.2",
    "zod": "3.25.67"
  }
}
